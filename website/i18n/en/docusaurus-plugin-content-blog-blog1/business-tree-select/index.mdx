---
date: 2025-02-02
sidebar_label: BusinessTreeSelect:1:Derived Components:1
---

# BusinessTreeSelect

Provides the same functionality as BusinessSelect, but works with **tree** data instead of flat lists.

Define once, use conveniently across your entire application.

Initialization example:

```js
import { BusinessTreeSelectBuilder } from 'react-admin-kit';

// Define type for code hinting
type TreeSelectType = 'org';

const BusinessTreeSelect = BusinessTreeSelectBuilder<TreeSelectType>(
  {
    apis: [
      {
        type: 'org',
        api: queryOrgFunction,
      },
    ],
  })

// Usage in pages
<BusinessTreeSelect type="org" />;
```

> Since it processes tree data, pagination is not needed. The API should return an array of objects, with name as display text and id as value by default.

## Basic Usage

`BusinessTreeSelect` extends [antd TreeSelect](https://ant.design/components/tree-select#api), supporting all its props.

<Previewer src="@@/BusinessTreeSelectBuilder/demos/basic-en/index.tsx"></Previewer>

## Cascading Relationships

- This example shows department options cascading from company selection.

- Use queryParams to pass parameters to the API.

<Previewer src="@@/BusinessTreeSelectBuilder/demos/queryParams-en/index.tsx"></Previewer>

## Clear Cache

Use `clearTreeSelectCache(type?: string)` to clear cache. Omitting `type` clears all caches.

<Previewer src="@@/BusinessTreeSelectBuilder/demos/clearCache-en/index.tsx"></Previewer>

## onLoad Event

Triggered when tree data finishes loading. Useful for scenarios like auto-selecting the first item.

<Previewer src="@@/BusinessTreeSelectBuilder/demos/onLoad-en/index.tsx"></Previewer>

## API

### BusinessTreeSelectBuilder

<API
  filePath="src/BusinessTreeSelectBuilder/index.tsx"
  name="BusinessTreeSelectBuilder"
></API>

### ApiType

<API name="BizTreeApi"></API>

### BusinessTreeSelect

<API name="BusinessTreeSelectSelf"></API>

[See additional properties](https://ant.design/components/tree-select#api)
